# macOS CPU runtime; install oemer==0.1.8 separately with --no-deps.
# Upstream oemer metadata names onnxruntime-gpu even on Mac; see local-development.md.
-r requirements-omr-common.txt
onnxruntime==1.18.1
