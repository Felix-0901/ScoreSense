# Windows Python 3.11 OMR. For macOS use requirements-omr-mac.txt.
# oemer declares this runtime dependency; CPU execution is available without CUDA.
-r requirements-omr-common.txt
onnxruntime-gpu==1.17.1
oemer==0.1.8
