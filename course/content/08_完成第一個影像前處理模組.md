# 第 8 章｜完成第一個樂譜影像前處理模組

前幾章我們分別做過讀圖、灰階、CLAHE、Otsu、Adaptive Threshold、角度估計與旋轉。

現在第一次把它們整理成**一個可以重複使用的功能**。

這會成為 ScoreSense 的第一塊真正專題積木。

## 學習目標

- 把前幾章影像處理步驟串成清楚流程。
- 使用 `dataclass` 保存多個回傳結果。
- 將讀圖、寫圖、角度估計、旋轉拆成小函式。
- 實作 `preprocess_score()`。
- 產生 `preprocessed.png` 與 `binary_preview.png`。

[TOC]

---

## 單元一｜先畫出資料流

我們想要：

```text
input.jpg
  ↓
讀取彩圖
  ↓
灰階
  ↓
CLAHE
  ↓
Otsu 反相遮罩
  ↓
估角度
  ↓
旋轉校正
  ↓
Gaussian Blur
  ↓
Adaptive Threshold
  ↓
輸出兩張圖
```

注意最後保留兩個結果：

1. `preprocessed.png`：較保守的灰階強化圖，給 OMR 使用。
2. `binary_preview.png`：讓人觀察二值化效果，主要給教學與除錯。

**不是所有處理結果都一定要拿去餵 OMR。**

---

## 單元二｜dataclass 是什麼？

如果一個函式要回傳：

```text
處理後圖片路徑
二值圖路徑
旋轉角度
```

可以回傳 tuple，但閱讀性較差。

```python
from dataclasses import dataclass
from pathlib import Path


@dataclass
class PreprocessResult:
    enhanced_path: Path
    binary_path: Path
    rotation_degrees: float
```

之後：

```python
result = preprocess_score(...)
print(result.rotation_degrees)
```

會比 `result[2]` 清楚很多。

---

## 單元三｜建立 `image_preprocessor.py`

建立資料夾：

```text
scoresense_course/
├── image_preprocessor.py
├── materials/
└── output/
```

先放入安全讀寫函式：

```python
from pathlib import Path
import cv2
import numpy as np


def read_image(path):
    path = Path(path)
    raw = np.fromfile(str(path), dtype=np.uint8)
    image = cv2.imdecode(raw, cv2.IMREAD_COLOR)

    if image is None:
        raise ValueError(f"無法讀取圖片：{path}")

    return image


def write_image(path, image):
    path = Path(path)
    path.parent.mkdir(parents=True, exist_ok=True)

    ext = path.suffix or ".png"
    ok, buffer = cv2.imencode(ext, image)

    if not ok:
        raise ValueError(f"無法編碼圖片：{ext}")

    buffer.tofile(str(path))
```

---

## 單元四｜加入前一章的兩個函式

把：

```text
estimate_skew()
rotate_image()
```

加入同一檔案。

函式名稱前面不必急著加 `_`。學生先理解功能。等第 19 章整理正式專案時，再討論 `_read_image` 這種「內部使用」命名慣例。

---

## 單元五｜完成 `preprocess_score()`

```python
from dataclasses import dataclass
from pathlib import Path
import cv2
import numpy as np


@dataclass
class PreprocessResult:
    enhanced_path: Path
    binary_path: Path
    rotation_degrees: float


# 前面放 read_image / write_image / estimate_skew / rotate_image


def preprocess_score(input_path, output_dir):
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)

    image = read_image(input_path)
    gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

    clahe = cv2.createCLAHE(
        clipLimit=2.0,
        tileGridSize=(8, 8),
    )
    enhanced = clahe.apply(gray)

    _, otsu_inv = cv2.threshold(
        enhanced,
        0,
        255,
        cv2.THRESH_BINARY_INV + cv2.THRESH_OTSU,
    )

    angle = estimate_skew(otsu_inv)
    enhanced = rotate_image(enhanced, angle)

    denoised = cv2.GaussianBlur(enhanced, (3, 3), 0)

    binary = cv2.adaptiveThreshold(
        denoised,
        255,
        cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
        cv2.THRESH_BINARY,
        31,
        15,
    )

    enhanced_path = output_dir / "preprocessed.png"
    binary_path = output_dir / "binary_preview.png"

    write_image(enhanced_path, enhanced)
    write_image(binary_path, binary)

    return PreprocessResult(
        enhanced_path=enhanced_path,
        binary_path=binary_path,
        rotation_degrees=angle,
    )
```

---

## 單元六｜不要只寫函式，要真的測

建立 `lesson08_test_preprocess.py`：

```python
from image_preprocessor import preprocess_score

result = preprocess_score(
    "materials/score_skewed.png",
    "output/preprocess_test",
)

print("處理後：", result.enhanced_path)
print("二值圖：", result.binary_path)
print("旋轉角度：", result.rotation_degrees)
```

確認：

```text
output/preprocess_test/preprocessed.png
output/preprocess_test/binary_preview.png
```

都有產生。

---

## 里程碑一（影像）｜現在學生已經完成什麼？

```text
任意樂譜照片
      ↓
preprocess_score()
      ↓
較乾淨、較水平的影像
+
二值化預覽
```

目前還完全沒有 OMR，也沒有 MusicXML。

這是刻意的。

學生先把「圖片怎麼整理」學會，再去理解「整理完要交給誰」。

---

## 挑戰練習

1. 加入最大寬度限制，圖片太大時縮小。
2. 把 CLAHE 的 `clipLimit` 變成函式參數。
3. 把 `31` 與 `15` 變成函式參數。
4. 建立 `debug=True`，只有除錯時才輸出 binary preview。

---

## 章末檢核

請畫出 `preprocess_score()` 的資料流，不看程式。

並回答：

1. 為什麼 enhanced 圖給 OMR，而 binary 先當 debug？
2. dataclass 解決了什麼閱讀問題？
3. 什麼情況會讓 `read_image()` 主動報錯？
4. 一個大函式為什麼仍要拆出小函式？
